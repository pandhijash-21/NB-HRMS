table:
  name: roles
  schema: public
array_relationships:
  - name: permissions
    using:
      foreign_key_constraint_on:
        column: role_id
        table:
          name: role_permissions
          schema: public
  - name: users
    using:
      foreign_key_constraint_on:
        column: role_id
        table:
          name: users
          schema: public
